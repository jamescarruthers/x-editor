{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'r=$(git rev-parse --show-toplevel 2>/dev/null || pwd); sh \"$r/.claude/graphify-setup.sh\"'"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'r=$(git rev-parse --show-toplevel 2>/dev/null || pwd); [ -f \"$r/.claude/graphify-guard.sh\" ] && sh \"$r/.claude/graphify-guard.sh\" search; exit 0'"
          }
        ]
      },
      {
        "matcher": "Read|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'r=$(git rev-parse --show-toplevel 2>/dev/null || pwd); [ -f \"$r/.claude/graphify-guard.sh\" ] && sh \"$r/.claude/graphify-guard.sh\" read; exit 0'"
          }
        ]
      }
    ]
  }
}
